<UserControl x:Class="KanaTrainer.Views.KanaTrainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KanaTrainer.Views"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" Grid.Column="1"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           TextAlignment="Center"
                           Style="{StaticResource KanaTaskStyle}"
                           Text="{Binding Task}"/>

                <Grid Grid.Row="2"
                      Margin="0 10 0 0"
                      MinWidth="100">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0"
                               Grid.Row="0"
                               Padding="2.5"
                               Margin="0 0 5 0">
                        <Run Text="{Binding TotalKanas, Mode=OneWay}"/>
                        <Run Text="."/>
                    </TextBlock>

                    <TextBox Grid.Column="1"
                             Grid.Row="0"
                             Padding="2.5"
                             Text="{Binding AnswerInput, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SkipKanaCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <TextBlock
                        Grid.Column="1"
                        Grid.Row="1"
                        TextAlignment="Center"
                        Padding="2.5"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource LastKanaReminderStyle}"
                        Visibility="{Binding LastTask, Mode=OneWay, Converter={StaticResource NullVisibilityConverter}}">
                            
                        <Run 
                            Style="{StaticResource LastKanaRunStyle}"
                            Text="{Binding LastTask, Mode=OneWay}" />
                        <Run 
                            Style="{StaticResource LastKanaRunStyle}"
                            Text=" => " />
                        <Run
                            Style="{StaticResource LastKanaRunStyle}"
                            Text="{Binding LastAnswer, Mode=OneWay}" />
                    </TextBlock>

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
