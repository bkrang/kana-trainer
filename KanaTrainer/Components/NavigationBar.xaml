<UserControl x:Class="KanaTrainer.Components.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KanaTrainer.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource NavigationBackgroundColor}">
    <UserControl.Resources>
        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource NavigationBarButtonStyle}" />
    </UserControl.Resources>
    <Grid Margin="20 10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <TextBlock
            FontSize="16"
            Foreground="White"
            Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=Title}"/>

        <StackPanel
            Orientation="Horizontal"
            Grid.Column="1">

            <Button
                Margin="10 0 0 0"
                Content="Word Training (WIP)"/>
            <Separator
                Margin="10 0 0 0"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Style="{StaticResource VerticalSeparatorStyle}"
                Width="1" />
            <Button
                Margin="10 0 0 0"
                Command="{Binding NavigateTrainingSettingsCommand}"
                
                Content="Training Settings"/>

        </StackPanel>
        
        
    </Grid>
</UserControl>
